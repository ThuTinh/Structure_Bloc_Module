name: core
description: A new Flutter package.
version: 0.0.1

publish_to: none

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # flutter_bloc: ^7.0.0

  dependencies:
    path: ../dependencies
  shared:
    path: ../shared
  